name: Build Telegram APK

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set up Java 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Build Configuration
      run: |
        rm -rf TMessagesProj/build .gradle build
        echo "sdk.dir=$ANDROID_HOME" > local.properties
        echo "APP_ID=94575" >> gradle.properties
        echo "APP_HASH=a3406de8d171326e3c403d80a9b00a9c" >> gradle.properties

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Telegram APK
      run: ./gradlew assembleAfatDebug --no-daemon --stacktrace

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Telegram-Custom-APK
        path: TMessagesProj/build/outputs/apk/**/*.apk
